@page "/moderation"
@inject HttpClient http
<div class="row">
    <div class="col-md-9">
        @if(Dictants != null)
        { 
            @foreach (var model in Dictants)
            {
                <Dictant.Web.Components.Moderation.ModerationItemComponent Source="@model"/>
            }
        }
    </div>
</div>